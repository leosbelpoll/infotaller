{% extends 'base.html.twig' %}

{% block title %}ClassVehicle{% endblock %}

{% block body %}
    <h1>ClassVehicle</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ class_vehicle.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ class_vehicle.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ class_vehicle.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('class_vehicle_index') }}">back to list</a>

    <a href="{{ path('class_vehicle_edit', {'id': class_vehicle.id}) }}">edit</a>

    {{ include('class_vehicle/_delete_form.html.twig') }}
{% endblock %}
